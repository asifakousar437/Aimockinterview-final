[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python ai_mock_interview/app.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "5000"
